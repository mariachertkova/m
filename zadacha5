#include<iostream>
#include<math.h>
#include<locale.h>
#include<cmath>
using namespace std;
int main(void){
	int sum=0, p2=1, i=1;
	do{
		cout<<p2<<(i<11?"+" : "");
		sum+=p2;
		p2*=2;
	}
		while(i++<=10);
		cout<<"="<<sum<<endl;
		system("pause");
		return 0;
}
